# Base Sepolia — QuickNode HTTPS endpoint
QUICKNODE_RPC="https://YOUR-ENDPOINT.base-sepolia.quiknode.pro/SECRET/"

# One line; 64 hex chars (optional 0x). No smart quotes, spaces, or trailing comments on the same line.
DEPLOYER_KEY=

# Optional: for `npm run set-game:base-sepolia` (or set in shell)
# CLICK_ADDRESS="0x..."
# GAME_ADDRESS="0x..."
